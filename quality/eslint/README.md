# eslint — JavaScript / Node.js 22 (React frontend)

Lints the real `src/` tree, including `.jsx` components (`--ext .js,.jsx`
is required — ESLint 8's default extension list is `.js` only).

Run from `frontend/`:
```
npx eslint src/ --ext .js,.jsx
```

Verified in this session — real output: clean (0 errors) under the
current config.
