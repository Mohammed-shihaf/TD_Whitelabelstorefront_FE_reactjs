# npm-cli — JavaScript / Node.js 22 (React frontend)

Verifies a clean, reproducible install from the real lockfile.

Run from `frontend/`:
```
npm ci
```

Verified in this session — real output: succeeded (exit 0).
