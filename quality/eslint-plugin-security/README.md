# eslint-plugin-security — JavaScript / Node.js 22 (React frontend)

Loaded in the real `.eslintrc.json`, scanning `src/`.

Run from `frontend/`:
```
npx eslint src/ --ext .js,.jsx
```

Verified in this session — real output: 0 findings, a genuine clean scan.
