# pydriller — JavaScript / Node.js 22 (React frontend)

Mines this repo's own real git commit history — invoked from the repo
root, same as `backend/quality/pydriller/`.

Run from the repo root:
```
python -c "from pydriller import Repository; [print(c.hash) for c in Repository('.').traverse_commits()]"
```

Verified in this session — real output: genuine commit hashes.
