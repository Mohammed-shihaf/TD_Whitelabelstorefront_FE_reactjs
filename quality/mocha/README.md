# mocha — JavaScript / Node.js 22 (React frontend)

Tests the real client-side pricing mirror in `src/pricing.js`.

Run from `frontend/`:
```
npx mocha "test/**/*.test.js"
```

Verified in this session — real output: 8 passing.
