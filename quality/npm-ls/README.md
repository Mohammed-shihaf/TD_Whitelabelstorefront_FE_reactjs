# npm-ls — JavaScript / Node.js 22 (React frontend)

Full dependency tree for the real `package.json`.

Run from `frontend/`:
```
npm ls --all --json
```

Verified in this session — real output: genuine tree for `storefront-web`.
