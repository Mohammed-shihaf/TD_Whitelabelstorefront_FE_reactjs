apiVersion: apps/v1
kind: Deployment
metadata:
  name: td-whitelabelstorefront-fe-reactjs
  labels:
    app: td-whitelabelstorefront-fe-reactjs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: td-whitelabelstorefront-fe-reactjs
  template:
    metadata:
      labels:
        app: td-whitelabelstorefront-fe-reactjs
    spec:
      containers:
        - name: app
          image: td-whitelabelstorefront-fe-reactjs:18
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: td-whitelabelstorefront-fe-reactjs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: td-whitelabelstorefront-fe-reactjs
